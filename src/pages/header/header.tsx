import React from 'react';
import Button from 'components/button';

const Header = () => {
  return (
    <header className="drag-header">
      <Button text="按钮" />
    </header>
  );
};

export { Header };
